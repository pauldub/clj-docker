# Ubuntu_TTY
#
# a dockerfile, which helps to initialize
# a new container that supports to attach to the feeds
# of the container.
# It's simplifies testing docker client against real API
# and makes possible to debug the source code.

FROM ubuntu
MAINTAINER Timo Sulg, timo@tauho.com



